# Berry-MRAG 当前阶段进度报告



---

## 1. 项目结论（TL;DR）
当前项目已实现可运行的“后端主链路”：
- 图像输入（路径或上传）-> 视觉识别（YOLO优先）-> RAG检索 -> 重排 -> 文本报告生成（Gemini/模板）-> API 返回。

当前项目**未进入正式模型阶段**：
- 你尚未进行真实数据标注；
- 也未进行正式可泛化的 fine-tuning。

当前最适合定位：
- **可演示 MVP（流程可用）**，不是**正式可上线模型**。

---

## 2. 范围与前提
1. 本报告基于当前仓库代码与近期联调结果。
2. 你已明确：暂不做标注，暂不做正式 fine-tuning。
3. 汇报重点是“当前做到了什么、还能演示什么、风险在哪里”。

---

## 3. 模块完成度总览

| 模块              | 状态        | 完成度 | 说明                                   |
| --------------- | --------- | --: | ------------------------------------ |
| 后端 API（FastAPI） | 已完成       | 90% | `health/diagnose/diagnose_upload` 可用 |
| 视觉推理（YOLO接入）    | 已完成（工程）   | 75% | 有真实推理与回退机制；精度依赖数据                    |
| YOLO训练脚本        | 已完成       | 85% | 训练流程可跑，能产出 `best.pt`                 |
| RAG检索（chunks）   | 已完成       | 85% | 本地知识块+向量检索+过滤                        |
| 向量缓存机制          | 已完成（本轮修复） | 80% | 已支持磁盘缓存复用，避免每次全量重算                   |
| Rerank          | 已完成（规则版）  | 70% | 基于 disease/crop/关键词等加权               |
| 生成模块（Gemini）    | 已完成（可回退）  | 80% | Gemini可用，失败回退模板                      |
| 评测与指标看板         | 部分完成      | 45% | 有离线评测脚本与报告，缺持续看板                     |
| 前端              | 进行中（外部协作） | 90% | 后期美化                                 |
| 正式数据与正式训练       | 未完成       | 15% | 你明确暂不推进                              |

---

## 4. 已完成的关键能力（按链路）

### 4.1 API层
已具备接口：
1. `GET /api/health`
2. `POST /api/diagnose`（JSON）
3. `POST /api/diagnose/upload`（FormData 上传）

已实现能力：
1. request_id/耗时日志；
2. 上传图片保存到 `data/raw`；
3. 结构化返回：`detection/retrieved/answer_markdown`。

---

### 4.2 视觉模块（YOLO）
已实现能力：
1. 优先本地 YOLO 推理；
2. 非业务类或低置信度自动回退 `unknown_leaf_issue`；
3. 环境变量可控：`YOLO_MODEL_PATH/YOLO_DEVICE/YOLO_CONF/YOLO_IOU/YOLO_BUSINESS_CONF`；
4. 当 YOLO不可用或异常时回退到文件名规则（保证服务可用）。

当前限制：
1. 业务类别白名单仅 3 类；
2. 无真实标注时，模型输出稳定性有限；
3. `unknown_leaf_issue` 是保护性逻辑，不是系统崩溃。

---

### 4.3 RAG 检索与向量化
已实现能力：
1. 检索源优先级：`data/chunks` -> `docs/berry_manual.md` -> 内置最小知识库；
2. 支持 `crop`、`disease_hint` 过滤；
3. 支持 Embedding（DashScope OpenAI兼容，`text-embedding-v4`）；
4. 接口不可用时回退哈希向量（服务不断）。

本轮关键修复（已完成）：
1. `retriever` 由“只写缓存”改为“优先读缓存”；
2. 缓存签名校验：`signature + dim + items/vectors 一致性`；
3. 启动新进程后若缓存有效，不再重复全量在线 embedding。

效果：
1. 首次重建（93条）约 1~2 分钟；
2. 后续冷启动命中缓存，embedding 调用可降为 0。

---

### 4.4 生成模块（MLLM）
已实现能力：
1. `GEN_PROVIDER=gemini` 时调用 Gemini；
2. 调用失败自动回退模板生成；
3. 输出为 Markdown 报告，包含检索证据和建议。

已知注意点：
1. 模型名需使用可用 ID（如 `gemini-flash-latest`）；
2. API Key/网络波动会影响生成速度与稳定性；
3. 线上演示建议保留 template 兜底开关。

---

## 5. 训练与数据状态（重点说明）

### 5.1 你当前“没有标注、不做正式微调”的现状
结论：
1. 可以稳定演示**流程能力**；
2. 不能承诺**识别精度指标**。

### 5.2 曾执行过的 smoke fine-tuning（性质说明）
已做过一次“流程打通训练”：
1. 用极少样本和临时框跑通训练脚本；
2. 成功产出 `best.pt`，证明训练链路与GPU链路可用；
3. 该结果不能代表可泛化识别能力。

这一步的价值：
1. 证明工程链路通；
2. 不用于正式精度结论。

---

## 6. 当前可演示能力（不做标注前提下）

可演示目标：
1. 输入图片和问题；
2. 系统完成识别->检索->生成；
3. 返回结构化结果和建议文本。

推荐演示策略：
1. 固定使用已测试图片；
2. 固定提问模板；
3. 保留 `disease_hint` 辅助（降低回退风险）；
4. 生成模块如不稳切 `template`。

---

## 7. 已识别问题与风险

### 7.1 风险清单
1. 视觉识别准确率无法对外承诺（无正式标注训练）。
2. `unknown_leaf_issue` 出现概率受阈值与模型质量影响。
3. 若误重建向量缓存，会出现首次请求时延偏高。
4. Gemini 外部依赖存在网络、配额、模型ID变更风险。
5. 当前部分文档中“预期目标”与“当前落地状态”存在天然差距。

### 7.2 风险等级（面向答辩）
1. 链路跑通风险：低
2. 演示稳定性风险：中
3. 精度质询风险：高（需要明确“当前阶段为MVP”）

---

## 8. 答辩话术建议（可直接用）
1. “本阶段目标是打通多模态闭环与工程可运行性，已完成端到端接口与模块联调。”  
2. “视觉模型已接入本地 YOLO 推理，支持阈值与业务规则控制，具备回退保护机制。”  
3. “RAG 已接入真实 embedding 与本地向量缓存，支持离线知识块和过滤检索。”  
4. “正式精度提升依赖下一阶段真实标注与系统化训练，本阶段不宣称最终识别精度。”

---

## 9. 当前代码与依赖状态

### 9.1 关键依赖（requirements）
1. `fastapi` / `uvicorn` / `pydantic`
2. `ultralytics` / `opencv-python`
3. `openai` / `requests`
4. `python-multipart`

### 9.2 当前仓库状态（本机）
1. 主要代码已可运行；
2. 当前 `git status` 显示存在未跟踪文件：`run_dev.py`（与本次核心功能无强耦合）。

---

## 10. 本阶段产出清单（可在答辩材料中引用）
1. 后端可运行接口：`/api/health`、`/api/diagnose`、`/api/diagnose/upload`
2. YOLO 推理与训练脚本：`visual_module/inference.py`、`visual_module/train_yolo.py`
3. RAG 关键模块：`rag_module/embedder.py`、`retriever.py`、`reranker.py`、`mllm_generator.py`
4. 数据与评测脚本：`build_chunks.py`、`eval_retrieval.py`、`generate_synthetic_rag_data.py`
5. 文档：
   - `README.md`
   - `docs/demo_finetune_plan.md`
   - `docs/data_annotation_spec.md`

---

## 11. 若保持“暂不标注、不正式训练”，下一步最优动作
1. 固定答辩演示素材与提问，不做临时变更。
2. 统一演示环境变量模板（YOLO阈值、GEN_PROVIDER、EMBEDDING配置）。
3. 演示前做 1 次完整预热请求，确认向量缓存命中。
4. 准备“能力边界说明页”：明确本阶段是 MVP 工程验证，不是最终精度版本。

---

## 12. 总结
项目当前已达到“可演示、可联调、可复现实验链路”的里程碑。  
在你明确不进行标注和正式 fine-tuning 的前提下，最优策略是：**聚焦展示系统工程闭环能力，而非模型精度结论**。

